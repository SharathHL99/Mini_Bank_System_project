spring:
  application:
    name: BankingMiniCoreSystem

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/bankingdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Sharathhl@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    com.banking: DEBUG
    org.hibernate.SQL: DEBUG